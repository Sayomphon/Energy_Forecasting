# Pinned for reproducibility (matches the environment the results were produced with).
# For a fresh setup prefer: pip install -e ".[dev]"
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
joblib==1.5.3
matplotlib==3.9.4
ucimlrepo>=0.0.7
pytest==8.4.2
